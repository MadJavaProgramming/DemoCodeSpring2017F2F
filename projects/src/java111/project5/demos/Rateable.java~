/** the rateable interface used to assign a rating to a business, product, etc.
 * @author pwaite
 */
 public interface Rateable {
     /* get the item's rating
      * @return the rating
      */
      public abstract String getRating();
      
     /* set the item's rating
      * @param rating represent the item's rathing
      */
      public abstract void setRating(String rating);
  
 } 

 
 
